
package model.fold;
import model.player.*;
import model.pawn.*;
import model.card.*;
import model.deck.*;

/**
 * Represents the fold mechanism in the game.
 * 
 * @author Zack csd4602
 */
public class fold {
    
    /**
     * Checks if a player can press the fold button based on the card and pawn.
     * 
     * @param card The card held by the player.
     * @param pawn The pawn of the player.
     * @return true if the player can press fold, false otherwise.
     * 
     * @pre The provided card and pawn are not null.
     * @post None.
     */
    public boolean checkIfFoldPossible(card card, pawn pawn){
        
        return true; //for now
    }
    
}
